{{include('partials/header.twig')}}

<div class="row">
    <div class="col s12 season">
        <img src="{{ seasons.image_season }}" height="500" width="350">
        <h3>Season {{ seasons.id }}</h3>
        <h4>Synopsis :</h4>
        <h5>{{ seasons.synopsis_season }}</h5>
        <h4>Number of episodes :</h4> 
        <h5>{{ seasons.episodes }}</h5>
    </div>
</div>

{{include('partials/footer.twig')}}
