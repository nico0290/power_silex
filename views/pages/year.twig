{{include('partials/header.twig')}}

<div class="row">
    <div class="col s12 year">
        <h2>P{{ years.graduation_year }}</h2>
    </div>
    <p>Étudiants en faisant parti :</p>
    {% for student in years.students %}

        <div class="col s3 year">
            <div class="card">
                <div class="card-content center-align">
                    <a href="{{url('student',{id:student.id})}}"> 
                        <h5>{{ student.first_name }} {{ student.last_name }}</h5>
                    </a>
                </div>
            </div>
        </div>
    {% endfor %}
</div>

{{include('partials/footer.twig')}}
